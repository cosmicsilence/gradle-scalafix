language: java
jdk: openjdk8

deploy:
  provider: releases
  api_key: $GITHUB_KEY
#  file: "FILE TO UPLOAD"
  skip_cleanup: true
#  on:
#    tags: true

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/